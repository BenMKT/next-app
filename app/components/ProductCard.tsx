import React from 'react';
import AddToCart from './AddToCart';

const ProductCard = () => {
  return (
      <div>
          {/* rest of the code before below button component */}
      <AddToCart />
    </div>
  );
};

export default ProductCard;
